name: Contribution terms (test rig caller)

# Sandbox only. Do not merge. Calls the test copy from this branch.

on:
  pull_request:
    types: [opened, edited, reopened, synchronize]
    branches: [main]

permissions: {}

jobs:
  terms:
    uses: embarquech/.github/.github/workflows/contribution-terms-test.yml@fix/staff-detection
